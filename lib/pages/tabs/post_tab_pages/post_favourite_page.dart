import 'package:flutter/material.dart';

class PostFavouritePage extends StatelessWidget {
  const PostFavouritePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('No Content to show ...'),
      ),
    );
  }
}
